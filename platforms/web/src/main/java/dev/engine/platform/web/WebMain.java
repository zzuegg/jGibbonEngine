package dev.engine.platform.web;

/**
 * Web entry point placeholder.
 *
 * <p>A proper deploy plugin will generate platform-specific entry points
 * automatically from shared game classes. For now, web deployment requires
 * a manual entry point that duplicates the game logic.
 */
public class WebMain {
    public static void main(String[] args) {
        System.out.println("Web deployment not yet configured. Use the deploy plugin (coming soon).");
    }
}

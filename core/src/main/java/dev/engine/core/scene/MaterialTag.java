package dev.engine.core.scene;

/** Phantom type tag for material handles in the scene. Opaque to the scene — only the renderer resolves these. */
public final class MaterialTag { private MaterialTag() {} }

package dev.engine.core.scene;

/**
 * Marker interface for all components.
 * Components are data attached to entities. They can be records (immutable)
 * or mutable classes — the ECS doesn't dictate.
 */
public interface Component {}

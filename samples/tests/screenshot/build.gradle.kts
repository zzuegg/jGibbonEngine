// Aggregator — the actual test code lives in the submodules:
//   :scenes  — shared scene definitions and discovery
//   :desktop — desktop screenshot tests (GL, VK, WebGPU-native)
//   :web     — browser screenshot tests (WebGPU via headless Chrome + CDP)
